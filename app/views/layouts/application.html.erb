<!DOCTYPE html>
<html lang="pt-br" class="dark">
  <head>
    <title><%= content_for(:title) || "Myplan" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 text-white">

    <!-- Desktop Navbar -->
    <nav class="bg-gray-800 p-4 hidden md:flex justify-between items-center">
      <%= link_to "Myplan", root_path, class: "text-xl font-semibold text-blue-500" %>
      <div class="flex space-x-4">
        <%= link_to "Objetivos", objectives_path, class: "text-white hover:bg-gray-700 px-4 py-2 rounded-md" %>
        <%= link_to "Novo Objetivo", new_objective_path, class: "text-white hover:bg-gray-700 px-4 py-2 rounded-md" %>
      </div>
    </nav>

    <!-- Mobile Navbar (Hamburguer Menu) -->
    <nav class="bg-gray-800 p-4 md:hidden">
      <div class="flex items-center justify-between">
        <%= link_to "Myplan", root_path, class: "text-xl font-semibold text-blue-500" %>
        <button id="mobile-menu-button" class="text-white" onclick="toggleMobileMenu()">
          <i class="bi bi-list"></i>
        </button>
      </div>
    </nav>

    <!-- Sidebar for Mobile with Smooth Transition -->
    <div id="mobile-menu" class="bg-gray-800 fixed top-0 left-0 w-64 h-full p-4 transform -translate-x-full transition-transform duration-300 ease-in-out">
      <%= link_to "Objetivos", objectives_path, class: "text-white hover:bg-gray-700 py-2 px-4 rounded-md" %>
      <%= link_to "Novo Objetivo", new_objective_path, class: "text-white hover:bg-gray-700 py-2 px-4 rounded-md mt-2" %>
    </div>

    <!-- Main Content -->
    <div class="md:ml-64 p-4">
      <%= yield %>
    </div>

    <script>
      function toggleMobileMenu() {
        const menu = document.getElementById("mobile-menu");
        menu.classList.toggle("-translate-x-full");  // Faz a animação de slide
      }
    </script>
  </body>
</html>
